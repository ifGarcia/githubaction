name: teste if

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  job_testes:
    runs-on: ubuntu-latest
    environment: DEV
#    if: ${{ github.ref == 'refs/heads/main' }}
    steps:
    - name: Ai ai aiai
      run: |
        tolower("AAAAAAAAA")
        tolower(${{ vars.ENVIRONMENT_VAR }})
        build_${{ vars.ENVIRONMENT_VAR }}.lowercase
        build_${{ vars.ENVIRONMENT_VAR }}.lowercase()
        ${{ vars.ENVIRONMENT_VAR }}.lowercase()
