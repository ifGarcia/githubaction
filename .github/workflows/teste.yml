name: Teste

on:
  workflow_dispatch:
  
env:
  branch: a
  
jobs:
  job_teste:
    runs-on: ubuntu-latest
    environment: ${{ env.branch }}

    steps:
      - name: Imprime oi
        run: |
          echo "oi"
          echo "${{ vars.ENVIRONMENT_VAR }}"
