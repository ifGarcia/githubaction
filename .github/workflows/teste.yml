name: Teste

on:
  workflow_dispatch:
  
env:
  branch: a
  
jobs:
  job_teste:
    runs-on: ubuntu-latest
    environment:

    steps:
      - name: Imprime oi
        run: |
          echo "oi"
          echo "${{ vars.ENVIRONMENT_VAR }}"
          echo "${{ env.branch }}"
